# -------------------------------------------------
# Custom Nginx configuration for static site
# -------------------------------------------------
server {
    listen 80;
    server_name _;

    # Root directory for static files
    root /usr/share/nginx/html;
    index index.html;

    # Serve static files and return 404 for missing resources
    location / {
        try_files $uri $uri/ =404;
    }
}